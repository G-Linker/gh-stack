# Changelog

## 3.4.4

Release branch for this playground. Stack #5 (`feat/stack-mock` … `stack-mock-5`) will rebase onto this branch instead of merging straight into `main`.

- Mock: retarget stacked PRs from `main` to `feat/release-3.4.4`
- Mock: cascading `gh stack rebase` after the trunk changes
